# --- Core Server & API ---
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
pydantic-settings==2.3.4
python-multipart==0.0.9
ujson==5.10.0

# --- Telegram Bot Framework ---
aiogram==3.10.0
aiohttp==3.9.5

# --- Database & Cache (Исправлено под Bothost) ---
aiosqlite==0.20.0
redis>=5.0.4
# aioredis больше не нужен отдельным пакетом, он внутри redis

# --- System & Performance ---
psutil==5.9.8
tzdata==2024.1
uvloop==0.19.0; sys_platform != 'win32'
